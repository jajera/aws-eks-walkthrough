export const glossary: Record<string, string> = {
  alb: 'Application Load Balancer — a Layer 7 load balancer used to expose Kubernetes services.',
  ami: 'Amazon Machine Image — the OS image used to launch EKS worker nodes.',
  argocd: 'ArgoCD — a declarative, GitOps continuous delivery tool for Kubernetes that syncs cluster state from a Git repo.',
  asg: 'Auto Scaling Group — automatically adjusts the number of EC2 instances in a node group based on demand.',
  aws: 'Amazon Web Services — the cloud platform used throughout this walkthrough.',
  az: 'Availability Zone — an isolated data-centre location within an AWS region used for high availability.',
  cidr: 'Classless Inter-Domain Routing — the IP address range notation (e.g. 10.0.0.0/16) used to define VPC and subnet sizes.',
  cli: 'Command Line Interface — a text-based interface used to interact with systems and tools by running commands in a terminal.',
  dynamodb: 'Amazon DynamoDB — a fully managed NoSQL key-value and document database designed for high availability, low latency, and massive scale.',
  ebs: 'Elastic Block Store — persistent block storage attached to EKS worker nodes, used for stateful workloads.',
  ecr: 'Elastic Container Registry — the AWS-managed Docker image registry.',
  eks: 'Elastic Kubernetes Service — AWS-managed Kubernetes control plane; you manage nodes and workloads, AWS runs the API server, etcd, and scheduler.',
  iam: 'Identity and Access Management — the AWS service that controls permissions and roles for all resources.',
  'iam-identity-center':
    'IAM Identity Center (formerly AWS SSO) — centralised identity and access for AWS and applications, with SSO and OIDC; used for human and GitOps (e.g. ArgoCD) access.',
  igw: 'Internet Gateway — the VPC attachment that enables public subnets to communicate with the internet.',
  irsa: 'IAM Roles for Service Accounts — lets Kubernetes pods assume an IAM role without sharing node credentials.',
  kubectl: 'kubectl — the Kubernetes command-line tool used to interact with the cluster after it is provisioned.',
  kubernetes: 'Kubernetes — an open-source container orchestration platform that automates the deployment, scaling, and management of containerised applications.',
  nat: 'NAT Gateway — allows instances in private subnets to reach the internet without being directly reachable from it.',
  nlb: 'Network Load Balancer — a Layer 4 load balancer, lower latency than ALB.',
  oidc: 'OpenID Connect — the federation protocol EKS uses to link IAM roles to Kubernetes service accounts (IRSA).',
  s3: 'Amazon S3 — an object storage service used to store and retrieve files, backups, logs, and static assets at virtually unlimited scale.',
  sg: 'Security Group — a stateful virtual firewall that controls inbound and outbound traffic for EC2 and EKS nodes.',
  subnet: 'Subnet — a segment of a VPC CIDR block in a single Availability Zone; EKS nodes and pods run in private subnets, NAT Gateways and load balancers in public subnets.',
  sso: 'Single Sign-On — one login for multiple apps or services; IAM Identity Center and other IdPs use SSO so you sign in once to access AWS or Git.',
  terraform: 'Terraform — the infrastructure-as-code tool used to create and manage all AWS resources in this walkthrough.',
  vpc: 'Virtual Private Cloud — an isolated, private network in AWS where your cluster resources run.',
};
